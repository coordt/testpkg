============
Installation
============

At the command line::

    $ easy_install django-testpkg

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-testpkg
    $ pip install django-testpkg
